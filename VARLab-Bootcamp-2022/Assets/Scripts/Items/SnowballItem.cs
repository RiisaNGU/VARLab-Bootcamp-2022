using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace VARLabBootCamp2022
{
    public class SnowballItem : MonoBehaviour, IItem
    {
        public void Item(GameObject item)
        {
            /* For each item make sure you:
             * check if the player is already holding an item -> or change it so the new item overrides the old one
             * pick the new item up
             * implement the controls + projectiles for the item
             */
        }
    }
}
